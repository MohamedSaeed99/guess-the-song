import styled from "@emotion/styled";
import { Box } from "@mui/material";


export const MusicPlayerSection = styled(Box)({
    width: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
})